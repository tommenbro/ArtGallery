import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '01_forelesning',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
